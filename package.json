{
  "name": "trimble-crane-clearance-extension",
  "private": true,
  "version": "0.1.0",
  "description": "Trimble Connect 3D Viewer extension: import a crane from 3D Warehouse, place it, and check swing clearance against a design.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "trimble-connect-workspace-api": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
